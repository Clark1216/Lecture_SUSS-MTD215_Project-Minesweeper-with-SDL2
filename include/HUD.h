#pragma once
#include <SDL.h>
#include <SDL_ttf.h>
#include "loadTexture.h"

//Contains number of flags(bombs/mines) remaining, a timer, and a menu button
class HUD {
    private:

    public:
        
};